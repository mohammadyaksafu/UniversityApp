import 'package:flutter/material.dart';

class EventClubScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Event & Club Management')),
      body: Center(
        child: Text('Display Event & Club Management features here.'),
      ),
    );
  }
}

