import 'package:flutter/material.dart';

class CampusNavigationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Campus Navigation & AR Map')),
      body: Center(
        child: Text('Display Campus Navigation & AR Map here.'),
      ),
    );
  }
}