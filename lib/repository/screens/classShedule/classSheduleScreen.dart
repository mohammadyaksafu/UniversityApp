import 'package:flutter/material.dart';

class ClassScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Class Schedules & Faculty Contacts')),
      body: Center(
        child: Text('Display Class Schedules & Faculty Contacts here.'),
      ),
    );
  }
}